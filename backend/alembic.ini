[alembic]
script_location = %(here)s/migrations
prepend_sys_path = %(here)s
path_separator = os

# No database URL or credentials here. env.py reads this application's Settings,
# or the explicit programmatic config.attributes['database_url'/'connection'].
