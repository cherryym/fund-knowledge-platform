# 2026-09-24 变更汇总

本次汇总 `ed78114` 之后同一轮工作的代码、合成测试、评估材料及说明。部分配套文件于前一日晚间生成，作为完整功能依赖一并纳入。只记录工程交付，不表示全库业务准确性或目标生产环境已通过验收。

## RAG 与答疑链路

- 重排响应完整校验后原子应用，保留失败和未评分候选的真实状态。
- 补齐编号组、例外、附件、脚注与明确跨文献引用的关联；定位回执绑定实际来源版本及原块 Hash。
- 补查复用元数据读取与权限快照，保留逐查询模型计算及完整阅读边界。
- 增加候选轨迹、阶段耗时、离线评估工具、合成反例及 40 题待核验候选模板；缺少专家 gold 时不伪造业务通过。
- 生成前提供主来源和适用边界信息，生成后保留可定位的结构疑点；不吞正文、不重写旧答案。
- Qwen 输出头与批次实验保留证据。双标签输出头及 batch8 未默认启用，不以实验推算宣传真实业务提速。
- 补齐生产构建与安装包的评估模块依赖；本次 Git 提交不等于重打安装包或部署生产。

专项说明：[链路优化](rag-chain-optimization-20260924.md)、[来源与答案核对](rag-repair-20260924.md)、[离线评估](rag-evaluation.md)、[Qwen 实验边界](qwen-label-head-20260924.md)。

## 智能答疑交互

- 检索、重排与耗时诊断统一放入默认折叠的“执行记录”，保留业务疑点提示。
- 正文引用改为文献图标及实际文献名称；悬停、键盘聚焦显示完整标题和定位，点击沿用冻结版本及原文块。
- 同一引用组按文献版本归并，跨文献分别显示，不完整引用不猜补。
- 引用浮层不再重建答案 DOM 或丢失焦点，兼容自动聚焦滚动与 Esc 关闭。
- 咨询历史与答疑区的分界线支持左右拖动、方向键、双击复位和按用户/空间记忆宽度；窄屏保护输入区，拖动不重新请求历史或生成答案。

## 提交前验证

- 后端全量复验：4935 PASS / 8 SKIP / 0 FAIL，进程退出 0；4 条依赖弃用提示保留。缺少外部数据库等条件的可选用例不算通过。
- 前端最近完整回归：946 PASS / 0 FAIL / 0 CANCELLED；TypeScript、Vite 与 Sites 构建通过。
- 部署工具测试：62 PASS / 14 SKIP。未配置实际 Compose/PowerShell 工具的可选用例保持跳过，不算目标 Windows/Oracle 部署通过。
- 本机页面验证过引用预览、准确文献打开、分界线双向拖动、复位、切页记忆和手机布局。
- 提交内容执行发布路径/本机绝对路径检查、Git 差异检查及独立 Gitleaks 脱敏扫描。

## 发布边界

真实文档、问答、数据库、OAuth 登录状态、密钥、模型权重、本机备份与测试输出目录不进入仓库。合成 JUnit 报告中的本机主机名已脱敏，测试计数、状态与时间未改写。当前提交任务没有新调用生成模型、推送远端或修改生产环境。
